# MUN-0020 mandate

Product hardening is committed through 46323d4 with real disposable PostgreSQL
evidence green; the CI runner bootstrap fix is 45847b8. PR #9 is open.

Remaining: privilege retirement and preflight contract closure. Get PR #9 green
and merged, verify deploy and live health as separate proofs (green CI is not
deploy, and deploy is not health), then take MUN-0020 through QA, compliance and
archive.

## Autonomy (operator-granted, ARCA-0194 epic)

Full autonomy. Do NOT ask the operator anything - they are waiting for the
finished epic, not for questions. Resolve every blocker, ambiguity and design
disagreement yourself: use a consilium for contested calls and research best
practices on the web for anything you do not know, recording findings in
`datarim/insights/`. Production is authorised: merge to main, deploy, restart
services.

Hard boundaries that remain (safety, not operator gates):
- Never print, echo, log, commit or transcribe a real credential value.
- Do not fund the DeepSeek account: the exposed key still authenticates
  (`/models` -> 200) and is inert only because the balance is exhausted.
  Topping up re-arms the leak. Revocation is console-only and stays with the
  operator.
- Prove what you claim. An unproven acceptance criterion is BLOCKED, never N/A,
  and never closed on a ticket alone.

Work continuously to completion: finish the work, commit, push, open the PR,
get CI green, merge, then run QA, compliance and archive.

