{
  "//": "CommonJS transpile for the one .cts test shim. The shim exists because @nestjs/throttler is CommonJS and cannot be resolved by jest's ESM loader; `import x = require(...)` only compiles under module: commonjs, which the ESM test config cannot provide.",
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "module": "commonjs",
    "moduleResolution": "node",
    "types": ["jest", "node"],
    "paths": { "@muneral/types": ["../../packages/types/dist/index"] }
  }
}
